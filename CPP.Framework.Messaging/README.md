# CPP.Framework.Messaging

Provides helper classes to simplify access to messaging systems, such as email.

## Features

- **Mail Transport Providers** — Abstraction layer for sending email via SMTP or SendGrid
- **Active Directory Integration** — Recipient resolution via `System.DirectoryServices.AccountManagement`
